local prefix = "packages.shzl.pkg.shadow.skills."

return {
  require(prefix .. "qizhi"),
  require(prefix .. "jinqu"),
  require(prefix .. "jianxiang"),
  require(prefix .. "shenshi"),
  require(prefix .. "juzhan"),
  require(prefix .. "feijun"),
  require(prefix .. "binglue"),
  require(prefix .. "huaiju"),
  require(prefix .. "yili"),
  require(prefix .. "zhenglun"),
  require(prefix .. "kuizhu"),
  require(prefix .. "kuizhu_active"),
  require(prefix .. "chezheng"),
  require(prefix .. "lijun"),
  require(prefix .. "chenglue"),
  require(prefix .. "shicai"),
  require(prefix .. "cunmu"),
  --require(prefix .. "mingren"),
  --require(prefix .. "zhenliang"),
}
